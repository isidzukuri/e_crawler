.panel.panel-default
  .panel-heading categories
  .panel-body
    - if is_admin?
      = link_to "Create", new_category_path, :class => 'btn btn-primary'
  .list-group
  = render partial: "categories/item", :collection => @items
  
